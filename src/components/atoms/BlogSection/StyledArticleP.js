import styled from "styled-components"
import { motion } from "framer-motion"

export const StyledArticleP = styled(motion.p)`
  color: var(--dark-bg);
  font-size: 10px;
  line-height: 1.3em;
  letter-spacing: 0.8px;
  font-family: "Poppins";
  margin-top: 26px;
  padding-bottom: 13px;
  width: 100%;
`
