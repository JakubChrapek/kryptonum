import styled from "styled-components"
import { motion } from "framer-motion"

export const StyledArticleGridDiv = styled(motion.div)`
  display: flex;
  padding: 0;
  flex-direction: column;
  /* border-bottom: 2px solid var(--black); */
  align-items: flex-start;
`
