import styled from "styled-components"
import { motion } from "framer-motion"

export const StyledArticleH3 = styled(motion.h3)`
  margin-top: 15px;
  padding-top: 11px;
  border-top: 2px solid var(--black);
  color: var(--dark-bg);
  font-size: 16px;
  font-family: "Poppins";
  line-height: 1.38em;
  font-weight: normal;
  width: 100%;
`
