import styled from "styled-components"
import { motion } from "framer-motion"

export const StyledCategoriesList = styled(motion.ul)`
  li {
    display: inline-flex;
    font-family: "Poppins";
    font-size: 18px;
    line-height: 2.67;
    color: #090909;
  }
`
